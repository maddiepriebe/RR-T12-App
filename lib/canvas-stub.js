// Empty stub to satisfy pdfjs-dist's optional 'canvas' peer dependency.
// pdfjs-dist uses canvas for Node.js server-side rendering; we only run
// it in the browser where the native Canvas API is available instead.
module.exports = {};
